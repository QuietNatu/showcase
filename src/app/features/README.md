# Features

## What's in here?

The **features** folder contains code specific to features.

## Rules

- All features should have an **index.ts** file
- Only import from features what is exposed by index.ts
- Features should use the same folder model as the main **app** folder - features -> feature1 -> (components, hooks, types, utils, etc)
