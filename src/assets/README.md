# Styles

## What's in here?

The **assets** folder contains static assets that are directly imported by the app.
These files will be part of the javascript bundle and a HTTP request is not required to load them. Place files here when you want them to appear immediately on page load (like icons).

## Rules

- Files that should be lazy loaded (like fonts) should go to the **public** folder.
