# Mocks

## What's in here?

The **mocks** folder contains mocks for entities, for requests made by the app or, in some rare cases, library mocks.

## Rules

- Avoid hardcoded mocks. Use **Chance** to generate mock data.
- Mocks of the test environment should go to the **test** folder instead
