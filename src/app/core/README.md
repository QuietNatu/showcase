# Core

## What's in here?

The **core** folder contains any kind of providers or services that should be global to the whole app (singletons).

## Rules

- Core providers/services should only be instantiated by the app's entrypoint (main.tsx)
