# Styles

## What's in here?

The **styles** folder is where **global** css styles are placed.

## Rules

- Prefer using css modules if your styles are specific to a component or feature.
- Non util files (files that produce css) should use layers to better encapsulate styles.
