{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "storybook": {
      "dependsOn": ["build"]
    },
    "storybook:build": {
      "dependsOn": ["build"]
    }
  }
}
